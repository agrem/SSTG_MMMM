#include <fstream>
#include <iostream>
#include <vector>
#include <iomanip>
#include <algorithm>
#include <map>

using namespace std;

int n = 0;
int s = 0;

void d(int x)
{
	cin >> n;
	vector<int> vec;
	n = n + 1;
	x = vec[n];
	s = n;
	while (s / 2 >= 1 || tab[s] > tab[s / 2])
	{
		int pom = tab[s];
	}
}

int main(int argc, char *argv[])
{
	return 0;
}
